import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    container: {
        padding: 20,
    },
    fab: {
        position: 'absolute',
        margin: 16,
        right: 0,
        bottom: 0,
    },
    dialog: {
        padding: 20
    }
});

export default styles;
